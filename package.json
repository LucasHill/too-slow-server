{
  "name": "too-slow-server",
  "version": "1.0.0-beta.1",
  "description": "A slow server that could be way faster.",
  "repository": "https://github.com/LucasHill/too-slow-server",
  "author": "Lucas Hill",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "pm2 start dist/server.js --watch -i max",
    "delete": "pm2 delete all",
    "benchmark": "./benchmark/benchmark.sh"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.1",
    "@types/express": "^4.17.1",
    "@types/jest": "^24.0.18",
    "@types/node": "^12.7.8",
    "@types/supertest": "^2.0.8",
    "jest": "^24.9.0",
    "nodemon": "^1.19.2",
    "supertest": "^4.0.2",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "pm2": "^3.5.1"
  }
}
